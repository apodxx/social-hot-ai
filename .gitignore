# Secrets
#
# .env 不进版本管理。密钥只在本地，部署到另一台机器时手工拷一次
# （见 README「密钥与部署」的清单）。
#
# 为什么不用"提交加密后的 .env"：加密并不能省掉手工搬运动作——age 私钥同样必须
# 手工搬到另一台机器（放仓库里就等于没加密），却还要在两台机器上各装一个工具。
# 所以对这个项目的目标（换台电脑部署）来说，直接拷 .env 更少步骤、也不会跟
# GitHub 的 push protection 反复对抗。
.env
# 其它 .env.* 变体也是本地的（临时覆盖用），只放行模板。
.env.*
!.env.example

# ⚠️ 目录规则一律**锚定到仓库根**（前导 `/`）。
#
# 这不是风格问题：未锚定的 `media/` 会匹配**任意层级**下叫 media 的目录，
# 于是把源码模块 `backend/app/services/media/` 一起排除了——它因此从未被提交，
# 换台机器全新 clone 直接 `ModuleNotFoundError: No module named 'app.services.media'`。
# 实测踩过：Jetson 上部署卡在这一步。
# 新增"忽略某个运行期目录"的规则时，**务必写前导斜杠**。

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
/backend/.venv/
/backend/venv/

# Local PostgreSQL cluster (Phase 2) — data, not source
/.pgdata/
/pg-server.log

# Node (Phase 7 frontend)
/frontend/node_modules/
/frontend/dist/
/frontend/.vite/

# Downloaded image material (Phase 9). Not source: it is a local cache of other
# people's pictures, they are large, and the provider URLs they came from expire.
/media/

# Runtime logs (backend/logs 由启动脚本写入)
/backend/logs/

# Editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
